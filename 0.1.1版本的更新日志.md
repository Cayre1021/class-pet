# 0.1.1 版本的更新日志

## 版本信息
- 版本号：`0.1.1`
- 适用范围：网页端、Windows 桌面端、Android 同步资源

## 相比之前版本新增和调整的内容

### 1. 修复教师管理重新进入时的验证逻辑
- 教师从首页每次重新点击“教师管理”时，都会再次进入密码验证页面。
- 记住的班级名称和教师姓名仍然会保留，但不会直接跳过密码验证。

### 2. 优化宠物孵化流程
- 宠物不再到达阈值后自动直接显示。
- 学生达到条件后，宠物蛋会进入“可孵化”状态。
- 进入互动界面后，需要点击宠物蛋才会正式破壳并展示宠物形象。

### 3. 增加随机宠物路线
- 宠物蛋孵化后不再只有单一路线。
- 现在会根据学生分配到不同的宠物类型，例如小鸟、兔子、小狗、小鱼、龙等。
- 不同页面统一使用同一套宠物显示逻辑，避免显示不一致。

### 4. 修复宠物背景色和宠物本体混色的问题
- 已调整破壳后宠物的背景配色。
- 不再直接复用容易混住的蛋壳背景色。
- 例如蓝色小鱼不会再配蓝底、绿色龙不会再配绿底。
- 教师端、学生查询页、大屏页、宠物互动卡片都会同步生效。

### 5. 调整宠物心情值机制
- 心情值仍然保持原本的随时间递减逻辑。
- 每次互动后，心情值只会随机恢复 `1% ~ 5%`。
- 取消了低心情时宠物变灰的效果，宠物会保持正常显示。

### 6. 教师工作台增强
- 新增删除学生功能，方便清理误录或测试数据。
- 新增强化版自定义加分项和扣分项功能。
- 教师可以自己填写行为名称和对应分值，并立即在工作台中使用。

### 7. 修复 EXE 中输入框和移动端样式问题
- 修复了桌面 EXE 中输入框无法正常聚焦、无高亮、无光标的问题。
- 优化了添加学生、自定义规则等输入区的尺寸和布局。
- 改善了手机端和较小屏幕下的按钮与输入框适配效果。

## 桌面端说明
- 本次重新打包的 `0.1.1` 64 位桌面版，已重新按正确的 Electron 桌面构建流程生成。
- 桌面版资源路径已恢复为适用于本地 EXE 的相对路径形式，避免之前因错误打包流程导致的白屏问题。

## 当前文件命名规范
- 64 位安装包：`Class Pet Setup 0.1.1 64-bit.exe`
- 32 位安装包：`Class Pet Setup 0.1.1 32-bit.exe`

## 说明
- Windows 7 不支持当前 Electron 33 运行时，因此即使是 32 位安装包，也不能保证可在 Windows 7 上运行。
- 当前重点可稳定使用的桌面版本为重新打包后的 64 位 `0.1.1` 安装包。
